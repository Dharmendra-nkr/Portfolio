version: '3.8'

services:
  portfolio:
    build: .
    container_name: portfolio-website
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
